name: saviour_1
description: "Saviours - Emergency Response & Traffic Coordination System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management & Architecture
  provider: ^6.1.2
  
  # UI & Styling
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  fl_chart: ^0.69.0
  
  # Config & Persistence
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  uuid: ^4.5.1
  
  # Media & Hardware
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

